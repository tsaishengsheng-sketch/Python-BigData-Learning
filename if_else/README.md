# 🔀 Logic Control (邏輯判定與分支架構)

本單元透過多樣化的情境實作，強化對分支邏輯與邊界條件的嚴謹處理。

### 🚀 核心開發實力
* **多重分歧處理**：靈活運用 `if-elif-else` 封裝複雜判斷，實作如 **BMI 診斷系統**、**閏年判定** 等具備多層條件的演算法。
* **數學模型轉化**：將現實邏輯轉為程式邏輯，實現 **十六進位轉換 (Hex Converter)**、**座標系判定** 及 **二次方程式求根 (Quadratic Solver)**。
* **動態互動設計**：開發 **猜數字遊戲 (Guess Number)** 的判定核心，包含數值範圍動態更新與使用者輸入驗證。
* **軟體穩定性驗證**：在開發中主動納入 **零值檢查 (Zero Checker)** 與 **除法安全性驗證**，展示預防例外錯誤的防呆思維。

### 📂 實作精華
1.  **精選作品**：[`SmartLife_Toolbox.py`](./SmartLife_Toolbox.py) - 整合健康檢查與決策邏輯的 CLI 互動工具。
2.  **歸檔練習**：位於 [`learning/`](./learning/) 資料夾，包含針對布林邏輯與條件分支的詳細實作紀錄。

---
*此 README 內容為自動化腳本輔助生成，紀錄個人 Python 邏輯工程學習路徑。*